local MediaplayerWidget = require('my.widgets.media-player.MediaplayerWidget')
local MediaplayerPopup = require('my.widgets.media-player.MediaplayerPopup')

return {
    Widget = MediaplayerWidget,
    Popup = MediaplayerPopup
}
