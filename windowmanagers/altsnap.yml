---
where: os.name == "windows"

depends:
  - packagemanagers.scoop

actions:

  - action: command.run
    command: powershell
    args:
      - scoop bucket add extras

  - action: command.run
    command: powershell
    args:
      - scoop install altsnap
