---
actions:
  - action: command.run
    where: os.name == "linux"
    command: rm
    args:
      - -rf
      - "{{user.home_dir}}/.config/vibepanel"

  - action: file.link
    where: os.name == "linux"
    from: vibepanel
    to: "{{user.home_dir}}/.config/vibepanel"
