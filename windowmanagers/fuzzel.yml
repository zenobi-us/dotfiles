---
actions:
  - action: command.run
    where: os.name == "linux"
    command: rm
    args:
      - -rf
      - "{{user.home_dir}}/.config/fuzzel"

  - action: file.link
    where: os.name == "linux"
    from: fuzzel
    to: "{{user.home_dir}}/.config/fuzzel"
