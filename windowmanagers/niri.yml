---
actions:
  - action: command.run
    where: os.name == "linux"
    command: rm
    args:
      - -rf
      - "{{user.home_dir}}/.config/niri"

  - action: file.link
    where: os.name == "linux"
    from: niri
    to: "{{user.home_dir}}/.config/niri"
