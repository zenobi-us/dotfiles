---
where: os.name == "linux"

actions:

  - action: package.install
    list:
      - rofi
      - rofimoji

  - action: command.run
    command: sh
    args:
      - ./windowmanagers/files/rofi/setup.sh
