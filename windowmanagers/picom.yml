---
where: os.name == "linux"

actions:
  - action: package.install
    name: picom

  - action: directory.copy
    from: picom
    to: "{{user.home_dir}}/.config"

  - action: command.run
    command: dnf
    args:
      - remove
      - compton
      - --assumeyes
    suoo: true
