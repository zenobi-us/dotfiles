---
where: os.name == "linux"

actions:
  - action: command.run
    command: dnf
    args:
      - remove
      - compton
      - --assumeyes
    suoo: true

  - action: package.install
    name: picom

  - action: file.copy
    from: picom.conf
    to: "{{user.home_dir}}/.config/picom.conf"
