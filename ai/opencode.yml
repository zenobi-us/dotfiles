---
depends:
  - devtools.opencode


actions:

  - action: file.link
    to: "{{user.home_dir}}/.config/opencode/agent"
    from: agents

  - action: file.link
    to: "{{user.home_dir}}/.config/opencode/command"
    from: commands

  - action: file.link
    to: "{{user.home_dir}}/.config/opencode/skills"
    from: skills

  - action: file.link
    to: "{{user.home_dir}}/.config/opencode/AGENT.md"
    from: AGENTS.md
