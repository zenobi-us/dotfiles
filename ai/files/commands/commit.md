Use the writing-git-commits skill to commit unstaged and staged changes in the current repository.

1. Unstage any staged files before continuing.
2. Analyse and plan to group changes into atomically logical commits.
3. Confirm with the user by printing a table of the proposed commit plan.
4. On Confirmation, write and execute an inline bash script to perform the operation in one bash tool call that creates the separate commits.


