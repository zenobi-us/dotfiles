# Presenterm Overview (via GitHub CLI)

Primary upstream:
- Repo: `mfontanini/presenterm`
- URL: https://github.com/mfontanini/presenterm
- Site: https://mfontanini.github.io/presenterm/
- Description: "A markdown terminal slideshow tool"
- License: BSD-2-Clause
- Default branch: `master`

Discovery evidence (`gh search repos presenterm`):
- `mfontanini/presenterm` is the dominant project by stars.

Key repo areas:
- `examples/` contains practical slide deck examples.
- `docs/src/` contains formal documentation sections.
- `config.sample.yaml` and `config-file-schema.json` define configurable behavior.
