---
where: |
  os.name == "linux" || os.name == "macos"

actions:
  - action: command.run
    command: sh
    args:
      - -s
      - |
        rm -rf $HOME/.{zshrc,zprofile,zshenv,zsh,zsh_history}
        ls -s $PWD/shells/files/zsh/.{zshrc,zprofile,zshenv,zsh,zsh_history} ~/
