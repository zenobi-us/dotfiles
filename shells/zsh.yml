---
where: os.name == "linux" || os.name == "macos"

actions:
  - action: command.run
    command: sh
    args:
      - -s
      - |
        rm -rf ~/.{zshrc,zprofile,zshenv,zsh,zsh_history}
        ln -s $PWD/shells/files/zsh/.{zshrc,zprofile,zshenv,zsh,zsh_history} ~/
