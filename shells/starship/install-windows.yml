---
where: os.name == "windows"

depends:
  - packagemanagers.scoop

actions:
  - action: command.run
    command: powershell
    args:
      - 'scoop install starship'

  - action: file.copy
    from: xresource
    to: '{{user.config_dir}}/starship.toml'
