---
where: os.name == "linux" || os.name == "macos"

actions:
  - action: command.run
    command: curl
    args:
      - -sS https://starship.rs/install.sh | sh

  - action: file.copy
    from: xresource
    to: '{{user.config_dir}}/starship.toml'
