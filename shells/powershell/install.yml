---
where: os.name == "windows"

depends:
  - packagemanagers.scoop

actions:

  - action: command.run
    command: mkdir
    args:
      - '{{ user.home_dir }}\Documents\WindowsPowerShell'

  - action: directory.copy
    from: profile
    to: '{{ user.home_dir }}\Documents\WindowsPowerShell'
