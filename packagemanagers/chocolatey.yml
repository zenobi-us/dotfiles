---
where: os.name == "windows"

depends:
  - packagemanagers.scoop

actions:
  - action: command.run
    command: scoop
    args:
      - bucket add dodorz_scoop https://github.com/dodorz/scoop

  - action: command.run
    command: scoop
    args:
      - install Chocolatey
