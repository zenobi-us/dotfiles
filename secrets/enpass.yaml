---

depends:
  - packagemanagers.scoop

actions:

  - action: command.run
    where: os.name == "windows"
    command: powershell
    args:
      - scoop bucket add extras

  - action: command.run
    where: os.name == "windows"
    command: powershell
    args:
      - scoop install enpass
