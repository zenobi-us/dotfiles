---
where: os.name == "windows"

depends:
  - packagemanagers.scoop
  - devtools.wsl2.enable

actions:

  - action: command.run
    command: powershell
    args:
      - scoop bucket add extras

  - action: command.run
    command: powershell
    args:
      - scoop install ssh-agent-wsl
