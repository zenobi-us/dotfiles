---
depends:
  - packagemanagers.scoop

actions:
  - action: file.copy
    from: gitconfig
    to: '{{user.home_dir}}/.gitconfig'
    template: true

  - action: command.run
    when: os.name == "windows"
    command: powershell
    args:
      - scoop install git
