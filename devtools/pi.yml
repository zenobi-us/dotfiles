---
actions:
  # Initialize git submodules (e.g., powerline-footer extension)
  - action: command.run
    dir: "{{env.DOTFILE_REPO_ROOT}}"
    command: git
    args:
      - submodule
      - update
      - --init
      - --recursive

  # remove managed child links/files only; keep runtime auth/cache/session state
  - action: command.run
    command: rm
    where: os.name == "linux" || os.name == "macos"
    args:
      - -rf
      - "{{user.home_dir}}/.pi/agent/AGENTS.md"
      - "{{user.home_dir}}/.pi/agent/AGENT.md"
      - "{{user.home_dir}}/.pi/agent/APPEND_SYSTEM.md"
      - "{{user.home_dir}}/.pi/agent/bun.lock"
      - "{{user.home_dir}}/.pi/agent/package.json"
      - "{{user.home_dir}}/.pi/agent/extensions"
      - "{{user.home_dir}}/.pi/agent/agents"
      - "{{user.home_dir}}/.pi/agent/themes"
      - "{{user.home_dir}}/.pi/agent/packages"
      - "{{user.home_dir}}/.pi/agent/bundles"
      - "{{user.home_dir}}/.pi/agent/settings.json"
      - "{{user.home_dir}}/.pi/agent/pi-sub-bar-settings.json"
      - "{{user.home_dir}}/.pi/agent/pi-sub-core-settings.json"
      - "{{user.home_dir}}/.pi/agent/fzf.json"
      - "{{user.home_dir}}/.pi/agent/skills.config.json"
      - "{{user.home_dir}}/.pi/agent/tsconfig.json"

  - action: command.run
    where: os.name == "windows"
    command: pwsh
    args:
      - -c
      - |
        remove-item -recurse -force -erroraction SilentlyContinue $env:LOCALAPPDATA/pi
        new-item -path "$env:LOCALAPPDATA/pi" -itemtype symboliclink -value "$(pwd)/devtools/files/pi"

  - action: command.run
    command: mkdir
    where: os.name == "linux" || os.name == "macos"
    args:
      - -p
      - "{{user.home_dir}}/.pi/agent"

  - action: file.link
    to: "{{user.home_dir}}/.pi/agent/AGENTS.md"
    from: pi/agent/AGENTS.md

  - action: file.link
    to: "{{user.home_dir}}/.pi/agent/APPEND_SYSTEM.md"
    from: pi/agent/APPEND_SYSTEM.md

  - action: file.link
    to: "{{user.home_dir}}/.pi/agent/packages"
    from: pi/agent/packages

  - action: file.link
    to: "{{user.home_dir}}/.pi/agent/bundles"
    from: pi/agent/bundles

  - action: file.link
    to: "{{user.home_dir}}/.pi/agent/agents"
    from: pi/agent/agents

  - action: file.link
    to: "{{user.home_dir}}/.pi/agent/extensions"
    from: pi/agent/extensions

  - action: file.link
    to: "{{user.home_dir}}/.pi/agent/package.json"
    from: pi/agent/package.json

  - action: file.link
    to: "{{user.home_dir}}/.pi/agent/bun.lock"
    from: pi/agent/bun.lock

  - action: file.link
    to: "{{user.home_dir}}/.pi/agent/settings.json"
    from: pi/agent/settings.json

  - action: file.link
    to: "{{user.home_dir}}/.pi/agent/fzf.json"
    from: pi/agent/fzf.json

  - action: file.link
    to: "{{user.home_dir}}/.pi/agent/skills.config.json"
    from: pi/agent/skills.config.json

  - action: file.link
    to: "{{user.home_dir}}/.pi/agent/tsconfig.json"
    from: pi/agent/tsconfig.json
  - action: file.link
    to: "{{user.home_dir}}/.pi/agent/pi-sub-bar-settings.json"
    from: pi/agent/pi-sub-bar-settings.json

  - action: file.link
    to: "{{user.home_dir}}/.pi/agent/pi-sub-core-settings.json"
    from: pi/agent/pi-sub-core-settings.json

  # Install extension dependencies
  - action: command.run
    dir: "{{env.DOTFILE_REPO_ROOT}}/devtools/files/pi/agent"
    command: bun
    args:
      - install
