---
where: os.name == "linux" || os.name == "macos"

actions:
  - action: file.copy
    from: .tool-versions
    to: '{{user.home_dir}}/.tool-versions'

  - action: cmd.run
    command: bash
    args:
      - asdf/files/tools/asdf/setup.bash
