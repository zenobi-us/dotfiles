---
where: os.name == "linux" || os.name == "macos"

actions:
  - action: directory.copy
    from: asdf
    to: '{{user.home_dir}}'

  - action: cmd.run
    command: bash
    args:
      - files/asdf/setup-asdf.bash
