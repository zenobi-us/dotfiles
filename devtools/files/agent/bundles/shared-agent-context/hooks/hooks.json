{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "set -o pipefail; bun run \"${CLAUDE_PLUGIN_ROOT}/cli.ts\" inject | jq -c '{continue: true, hookSpecificOutput: {hookEventName: \"SessionStart\", additionalContext: (. // \"\")}}'"
          }
        ]
      }
    ]
  }
}
