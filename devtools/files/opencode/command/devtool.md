# Chrome Dev Tools

Perform actions using Chrome DevTools Protocol.

<UserRequest>
$ARGUMENTS
</UserRequest>

**Examples:**

```
task("Chrome Debug Subagent")  List the available browser tabs
task("Chrome Debug Subagent")  Get the current page title
```

This command delegates to the Chrome Debug Subagent via the task tool to provide deep debugging capabilities.
