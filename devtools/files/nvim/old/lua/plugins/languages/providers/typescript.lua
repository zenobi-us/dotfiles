return {
    tsserver = {
        on_attach = function(client, bufnr) end,
        settings = {
            completions = {
                completeFunctionCalls = true
            }
        }
    }
}
